# Database Scripts

Utility scripts for database management and debugging.

## Scripts

- **check-db.ts** - Inspect database contents (files, sessions)
  ```bash
  bun run scripts/db/check-db.ts
  ```

## Usage

Run these scripts directly with `bun`:
```bash
bun run scripts/db/check-db.ts
```
